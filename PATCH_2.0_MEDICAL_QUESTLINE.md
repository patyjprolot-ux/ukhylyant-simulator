# Медична гілка (PATCH 2.0, Р18 — розширена версія)

Дизайн-документ ПЕРЕД кодом (домовленість 2026-08-16). Замінює/розширює
вже задеплоєний QTE-медком (`routes/security.js`, коміт `54e1a4f`) —
той механізм **лишається як є** й стає фундаментом для "здачі аналізів"
нижче, нічого з нього не викидаємо.

## Що вже є (не чіпаємо)

- `/api/medcom/*` — QTE-тап по картці симптому, 3/5 влучань проходить
  повістку, постійна "Медична картка" (0-10) дає 7 днів щита на 10/10.
- Це лишається як швидкий, "разовий" спосіб відповісти на повістку.
- Нижче — ПАРАЛЕЛЬНА, довгограюча гілка: колекція Хвороб, які роблять
  той самий QTE легшим/надійнішим, і є однією з вимог легалізації.

## Основний цикл

```
Вилазка "Лікарня" (нова, підвищений ризик)
  → Направлення (referral, ресурс, quest-only)
      → Здати аналізи (QTE-міні-гра, той самий рушій що в medcom,
        але окремий набір карток на кожну хворобу) — коштує
        гроші + енергію + вигорання (той самий лічильник, що в Спринтах)
          → прогрес хвороби +1 (з N потрібних успішних здач)
              → на N/N — Хвороба скрафтена, назавжди у медичній картці
                  → дає ПОСТІЙНИЙ бонус до QTE-медкому (не гарантія,
                    шанс) + декоративний запис у "Твоїй справі"
```

## 1. Вилазка "Лікарня" (нова, `catalog/expeditions.js`)

```js
{ id: 'hospital', name: 'Черга в поліклініці', emoji: '🏥', minutes: 300,
  minLevel: 3, risk: 0.40, loot: 'referral' }
```
Ризик 0.40 — між `border` (0.35) і `tcc_office` (0.45), свідомо високий:
це навмисно найризикованіша вилазка після ТЦК, бо "лікарня" — це теж
місце, де можуть перевірити документи. 300 хв (5 год) — між `village`
(240) і `tcc_office` (600), середньодовга.

Дроп: 1-2 `referral` за успішну вилазку (як і решта вилазок — min/max
з вагами, конкретні числа підбираються під баланс, не вигадую зараз
навмання).

## 2. Ресурс "Направлення" (`referral`, `catalog/resources.js`)

```js
{ id: 'referral', name: 'Направлення до лікаря', emoji: '📝', tier: 3,
  sell: 0, tradeLock: 'quest' }
```
`sell: 0` — принципово не товар (як `route`/`script`/`intel_data`,
Р17). Єдине джерело — вилазка "Лікарня".

## 3. Здача аналізів — QTE, параметризований на хворобу

Технічно той самий рушій, що вже є в `lib/mechanics/` для medcom
(підсвітити картку → тапнути вчасно), але:
- Кожна хвороба має СВІЙ набір "аналізів" (карток) — тематичні назви
  під саму хворобу, не загальний `SYMPTOMS`.
- Вартість входу: `referral` (1 шт) + гроші (росте з тіром хвороби) +
  енергія (як Спринт) + вигорання (той самий `user.burnout`, що в
  Спринтах — ще одна причина, чому це "довгограюча" гілка: не можна
  спамити аналізи без відпочинку).
- Успіх → `user.diseaseProgress[diseaseId] += 1`.
- Дедлайн і кількість раундів — менші за медком (це рутинна дія, не
  разова подія): 3 раунди, 2 влучання з 3 — успіх.

## 4. Хвороби — 10 штук, 5 тірів по 2

Тір росте з рівнем схрону, де хвороба стає доступною (той самий дух,
що ешелони апгрейдів). Кожна хвороба: назва (сатира), скільки успішних
здач аналізів потрібно (N), вартість входу за спробу, бонус-ефект.

| Тір | Схрон | Хвороба | N здач | Вартість/спроба | Ефект володіння |
|---|---|---|---|---|---|
| 1 | 2 | 🦶 Плоскостопість documented | 3 | 500₴ | +5% шанс QTE-медкому |
| 1 | 2 | 🦴 Сколіоз офіційний | 3 | 500₴ | +5% шанс QTE-медкому |
| 2 | 3 | 😰 Панічний розлад із довідкою | 5 | 2000₴ | +8% шанс, −5% heat-штраф за провал |
| 2 | 4 | 💊 Хронічна алергія на все | 5 | 2000₴ | +8% шанс, −5% heat-штраф за провал |
| 3 | 5 | 🫀 Аритмія під наглядом | 7 | 8000₴ | +12% шанс, +200мс вікна QTE |
| 3 | 5 | 🧠 Синдром хронічної втоми | 7 | 8000₴ | +12% шанс, +200мс вікна QTE |
| 4 | 6 | 🦵 Грижа міжхребцева задокументована | 10 | 25000₴ | +18% шанс, −10% heat-штраф |
| 4 | 7 | 👁️ Прогресуюча міопія | 10 | 25000₴ | +18% шанс, −10% heat-штраф |
| 5 | 7 | 🩺 Букет хронічних (усі попередні як передумова) | 15 | 60000₴ | +25% шанс, +400мс вікна |
| 5 | 8 | 👑 **Синдром хронічної потужності** | 20 | 150000₴ | +35% шанс, потрібен для легалізації |

**"Букет хронічних"** (тір 5, перша) — навмисно вимагає ВСІХ 8
попередніх хвороб як передумову (не тільки здач аналізів) — це і є
"колекційна" вершина перед фінальною. **"Синдром хронічної
потужності"** — сама фінальна, сатирична назва обіграє слово
"потужність" (термін, що давно став мемом), символізує "офіційно
підтверджену невичерпну потужність" як абсурдний медичний діагноз.

Бонуси **сумуються** (не тільки остання) — власник усіх 10 має
+5+5+8+8+12+12+18+18+25+35 = **+146% шанс QTE-медкому** (з капом,
бачити нижче) і суттєве зниження heat-штрафу за провал. Це навмисно
робить пізню гру "майже завжди проходиш медком", що логічно (ти вже
офіційно хронічно хворий на все).

**Кап шансу QTE:** базовий поріг медкому (3/5 влучань) не міняємо,
натомість бонус діє як **додаткові "безкоштовні" влучання** — тобто
`effectiveHitsNeeded = max(1, MEDCOM_HITS_NEEDED - floor(totalBonus / 25))`.
При +146% це `3 - floor(146/25) = 3-5 = 0`, тобто гарантований прохід
на максимальній колекції — саме so задумано як нагорода за 10/10
хвороб, а не забаганка.

## 5. Легалізація — нова вимога

`/api/prestige/claim` (уже one-time, Р6+уточнення 2026-08-16) додатково
перевіряє `user.diseases['chronic_power'] === true` — без фінальної
хвороби легалізація недоступна навіть якщо `totalEarned` вистачає.
Повідомлення про відмову явно каже, чого бракує (аналогічно вже
наявному повідомленню про рівень схрону).

## 6. Нові поля стану (`lib/user-store.js`, `createFreshUser`)

```js
diseaseProgress: {},  // { diseaseId: кількість успішних здач аналізів }
diseases: {},          // { diseaseId: true } — скрафтовані назавжди
```
+ `RESTORE_MAP_FIELDS` (обидва — це словники з відомими ключами,
той самий патерн, що `skills`/`reputation`).

## 7. Що НЕ вирішую зараз (потребує тебе)

- **Точні min/max дропу `referral`** з вилазки — підбирається разом із
  рештою балансу вилазок, не вигадую навмання.
- **Точні набори "карток аналізів"** для кожної з 10 хвороб (тематичні
  назви симптомів під кожну) — творча робота, як і схрон-лор, зробимо
  разом (можу підготувати промпт для ChatGPT, як для лору переходів).
- **Чи бонус QTE діє й на майбутні механіки** (окрім медкому) — поки
  тільки медком, розширення на інше — окремий крок.

## Порядок реалізації (коли схвалиш документ)

1. Ресурс `referral` + вилазка `hospital` (найменший шматок, ізольований).
2. Дані 10 хвороб (`catalog/diseases.js`, новий файл) + поля стану.
3. Ендпоінти здачі аналізів (`/api/disease/start`, `/api/disease/tap`) —
   копія QTE-рушія medcom, параметризована.
4. Бонус-формула в `/api/medcom/tap` (додає ефект володіння хворобами).
5. Вимога `chronic_power` у `/api/prestige/claim`.
6. Клієнтський UI — окремий екран "Медична картка" (список хвороб,
   прогрес, кнопка "Здати аналізи").

Кожен крок — окремий коміт, живий curl-тест перед пушем (той самий
процес, що весь сьогоднішній день).
