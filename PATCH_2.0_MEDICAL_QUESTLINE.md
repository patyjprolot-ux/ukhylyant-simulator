# Медична гілка (PATCH 2.0, Р18 — версія 3, 2026-08-16)

⚠️ **Версії 1 і 2 цього документа були архітектурно неправильні** —
прив'язували медкомісію до реакції на повістку (QTE-тап). Розробник
прямо заборонив: *"При отриманні повістки ніякої гри, ніяких тапів,
ніяких карток, пов'язаної з хворобами чи мед комісією бути не
повинно... Повістка — серйозна річ"*. Код-наслідок цієї помилки вже
відкочено (`49d67bd`) — `/api/notice/resolve` більше не викликає
medcom. QTE-рушій (`/api/medcom/*`) лишається в коді як інженерна
заготовка для "здачі аналізів" нижче, просто зараз нізвідки не
викликається.

**Ця версія:** медкомісія — окрема сюжетна вкладка, повністю не
пов'язана з повістками.

## Основний цикл

```
🏥 Вкладка "Медкомісія" (нова, незалежна від локацій/вилазок для прокачки)
  │
  ├── Кнопка "Вилазка в лікарню" (незалежна вилазка: НЕ дає паперу/
  │   консервів/чогось прокачувального — тільки поглинає гроші/енергію/
  │   ресурси, дає направлення + документ для АКТИВНОЇ хвороби)
  │
  ├── Одна АКТИВНА хвороба за раз (user.activeDisease) — обмеження темпу,
  │   не можна прокачувати кілька паралельно
  │
  ├── "Здати аналізи" — платна дія (гроші+ресурси+енергія+вигорання),
  │   рухає прогрес аналізів активної хвороби
  │
  ├── Коли зібрано ВСІ документи + ВСІ аналізи активної хвороби →
  │   крафт (у тій самій вкладці) → хвороба "діагностована"
  │   (user.diseasesDiagnosed[id]=true), АЛЕ ще не в медичній картці
  │
  └── Ще одна вилазка в лікарню ПІСЛЯ крафту → хвороба переходить у
      медичну картку (user.diseases[id]=true) → активний слот звільняється
      → можна почати наступну хворобу
```

## 1. Вилазка "Лікарня" — незалежна від інших локацій

Не частина ротації звичайних вилазок (`catalog/expeditions.js` лишається
як є) — окремий тип, доступний з 🏥-вкладки будь-де, незалежно від
поточного рівня схрону (щоб гілка йшла паралельно з 1 по 7 схрон, а не
чекала конкретної локації).

```js
// catalog/hospital.js (новий файл)
HOSPITAL_VISIT: {
    minutes: 240,           // 4 год — досить довго, щоб не спамити
    cost: { tk: /* росте з тіром активної хвороби */, energy: 30 },
    riskFail: 0.15,         // шанс "нічого не привіз" (не небезпека спіймання,
                             // а звичайна невдача — лікарня не про розшук)
},
```
**Свідомо БЕЗ ризику облави/розшуку** — на відміну від бойових вилазок,
"чекати в черзі в поліклініці" не мало б підвищувати heat. Якщо треба
навпаки (лікарня теж підозріла) — скажи, поправлю на реальний risk%,
як у бойових вилазках.

Результат успішної вилазки:
- Якщо `activeDisease` немає (порожній слот) І `referral` = 0 → дає
  1 `referral` (можна почати нову хворобу).
- Якщо `activeDisease` є, і документи ще не всі зібрані → дає 1
  одиницю ПОТРІБНОГО документа цієї хвороби (не випадкового).
- Якщо хвороба вже "діагностована", але ще не в картці → саме ця
  вилазка й переносить її в картку (окрема гілка результату).

## 2. Ресурс "Направлення" (`referral`)

```js
{ id: 'referral', name: 'Направлення до лікаря', emoji: '📝', tier: 3,
  sell: 0, tradeLock: 'quest' }
```
Кап зберігання — 1 (не можна накопичити купу направлень наперед і
почати кілька хвороб одразу — ще один важіль обмеження темпу поверх
`activeDisease`-слоту).

## 3. Документи — специфічні під кожну хворобу

Кожна хвороба вимагає 2-4 типи документів (кількість росте з тіром),
кожен — окремий `quest`-ресурс, отримуваний ЛИШЕ з вилазки в лікарню,
поки ця хвороба активна. Конкретні назви документів (аналог
"Направлення на аналізи", "Виписка з амбулаторної картки" тощо) —
розділ 7 нижче, це не я вигадую зараз навмання.

## 4. Здача аналізів

Платна дія в тій самій вкладці, рушій — уже готовий QTE (`/api/medcom/*`,
перейменується на щось на кшталт `/api/disease/tap`), але:
- Кожна хвороба — свій набір карток "аналізів" (тематичні назви,
  розділ 7).
- Вартість ЗА СПРОБУ: гроші (росте з тіром) + ресурси (2-3 типи,
  залежно від хвороби) + енергія + `user.burnout` (той самий
  лічильник, що в Спринтах — і це ключовий важіль темпу: набурнячись
  на аналізах, треба чекати відновлення, як і в Спринтах, а не
  спамити нескінченно).
- Успіх (2/3 влучань, вікно коротше за старий медком — рутинна дія) →
  `user.diseaseAnalysisProgress[diseaseId] += 1`.

## 5. Крафт хвороби

Коли `diseaseAnalysisProgress[id] >= needed` І всі документи зібрані →
кнопка "Поставити діагноз" у вкладці. Миттєвий крафт (не вилазка) —
переводить у стан "діагностовано, чекає підтвердження в лікарні"
(розділ вище, крок 5 основного циклу).

## 6. Обмеження темпу — три незалежні важелі, не один

Розробник прямо попросив розтягнути весь ланцюжок 1→7 схрону, не дати
проскочити швидко. Три важелі одночасно (а не один, щоб жоден окремий
множник грошей/скіллів не міг обнулити темп):
1. **Один активний слот хвороби** — головний важіль, структурний, не
   обходиться жодним фармом.
2. **Кап направлень = 1** — не можна припасти наперед.
3. **Вигорання на аналізах** — той самий лічильник, що в Спринтах,
   природно обмежує, скільки спроб здачі аналізів можна зробити
   поспіль без відпочинку.
Разом: типовий час однієї хвороби тіру 1 (мало документів, мало
аналізів) — кілька годин активної гри врозкид; тіру 5 (Букет
хронічних, 8 передумов) — по конструкції розтягується на весь
попередній прогрес.

## 7. Що я НЕ вирішую сам (потребує тебе або текстової моделі)

Розробник прямо сказав: якщо не можу продумати лор — казати, а не
"висмоктувати з сірника". Чесно:

- **Назви й тексти 10 хвороб** (сатиричні, як "Синдром хронічної
  потужності" — цю ти вже дав сам) — попередній список я написав сам
  нашвидкуруч ("Плоскостопість documented" і т.д.) і не впевнений, що
  це досить якісно для гри, де ти явно хочеш продуманий лор.
- **Пакети документів під кожну хворобу** (назви, кількості, чому саме
  ці документи логічно потрібні для саме цієї хвороби).
- **Тематичні картки "аналізів"** для QTE кожної хвороби (аналог
  `SYMPTOMS`, але своя тема на кожну).
- **Наратив вилазки "Лікарня"** (флейвор-тексти черги, лікаря,
  бюрократії) — та сама якість, що вже є в `SYMPTOMS`/`REVENGE_LINES`.

**Пропозиція:** підготую розгорнутий промпт для ChatGPT (як робили для
схрон-лору), що включає весь контекст (тон гри, вже наявні приклади
сатири з коду, структуру що потрібно — 10 хвороб × документи ×
аналізи), і ти проженеш його сам або скажеш мені це зробити. Це
чесніше, ніж я сам видам щось середнє.

## 8. Легалізація — фінальний крафт

`user.diseases['chronic_power'] === true` — обов'язкова умова, разом
з іншими інгредієнтами (Білий Квиток? документи? репутація? — теж
потребує твого рішення, які саме "ключі" крім хвороби входять у
"довідку легалізації" як предмет крафту, а не просто перевірку
totalEarned, як зараз).

## 9. Схрон 8 — розблоковується ЛИШЕ після легалізації

**Зміна порядку геймплею** (зараз навпаки): `catalog/locations.js`
рівень 8 стає недоступним для купівлі, поки `user.prestigeCount < 1`.
`PRESTIGE_UNLOCK_LEVEL` (зараз 8, вимога для легалізації) змінюється
на 7 — легалізуватись можна з рівня 7 схрону, а сама легалізація і є
ключем, що відкриває купівлю рівня 8. Технічно: `/api/location/buy`
для рівня 8 отримує додаткову перевірку `user.prestigeCount >= 1`.

## 10. Переїзди між схронами 1-7 — мають стати справжніми квестами

Те, що я зробив у Фазі 3 (лор-текст при купівлі локації) — **це
недостатньо**, ти це прямо сказав ("не бачив продуманого лору", "не
бачив новин у квестах"). Поточний стан — просто спливаюче
повідомлення, не квест зі структурою/кроками/умовами. Це окрема
робота, потребує окремого дизайн-проходу (які саме кроки/умови на
кожен перехід, не тільки текст) — не змішую з медичною гілкою в цьому
документі, щоб не роздувати один файл на дві великі теми. Зроблю
окремий документ, коли дійде черга — скажи, коли починати.

## Порядок реалізації (коли схвалиш структуру вище)

1. `catalog/hospital.js` + ресурс `referral` — ізольований шматок.
2. Поля стану (`activeDisease`, `diseaseAnalysisProgress`, `diseases`,
   `diseasesDiagnosed`, документи-ресурси).
3. Промпт для лору (розділ 7) → чекаю контент → вписую в
   `catalog/diseases.js`.
4. Ендпоінти вилазки в лікарню + здачі аналізів + крафту.
5. Гейт схрону 8 (розділ 9) + вимога легалізації (розділ 8).
6. Клієнтський UI — вкладка "🏥 Медкомісія".

Кожен крок — окремий коміт, живий curl-тест перед пушем.
