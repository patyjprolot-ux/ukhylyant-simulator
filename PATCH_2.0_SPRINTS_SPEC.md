# Спринти — технічна специфікація (feature-flag SPRINTS_V2)

Синтез усіх рішень цієї сесії (Р0/Р1/Р2/Р3/Р4/Р5), нічого не вигадано заново.
Мета цього заходу — Фаза 1 з плану впровадження: додати систему ІЗОЛЬОВАНО,
за фіче-флагом, паралельно зі старим клікером, без остаточного тюнінгу чисел
(те — Фаза 2, окремий економічний тест на живих сценаріях).

## Модель (Р0): гібрид

Схрон 1 — старий клікер (`clickVal`/`passive`) без змін. Схрон 2-8 —
Спринти повністю замінюють прямий клік по артворку.

## Цифрові ресурси (нові, `catalog/resources.js`)

```js
{ id: 'script', name: 'Фрагмент коду', tier: 3, sell: 2500 }
{ id: 'intel_data', name: 'Розвідані дані', tier: 3, sell: 12000 }
{ id: 'crypto_key', name: 'Крипто-ключ', tier: 4, sell: 45000 }
```
(ціни DeepSeek, ніхто не заперечував; `crypto_key` тір 4 — як і `ticket`,
НЕ продається на біржі напряму, тільки крафт/контракти).

## Таблиця контрактів (Р1: 3-way бленд)

Фіксовані тіри як UI-ярлики (не окрема економічна модель кожен), але
винагорода рахується rate-формулою `(тривалість_сек/3600) × ставка(тір)` —
блендуємо "зрозумілі тіри" Grok з "просто масштабувати" DeepSeek:

| Тір | Схрон | Енергія входу | Тапів | Дедлайн | Ставка ₴/год | Дроп спец-ресурсу |
|---|---|---|---|---|---|---|
| Junior | 2-3 | 5 | 40 | 20 хв | 540 | 1× script (100%) |
| Middle | 4-5 | 25 | 180 | 50 хв | 10800 | 1× script + 1× intel_data (70%) |
| Senior | 6-7 | 45 | 450 | 3.5 год | 15700 | 2× intel_data + 1× crypto_key (40%) |
| Lead | 8 | 70 | 1100 | 10 год | 28000 | 2× crypto_key (20%) |

Енергія входу — навмисно різна "вартість входу в часі" (рішення в Р1):
Lead особливо дорогий і рідкісний, Junior можна повторювати часто.

## Вигорання (Burnout) — Р2, повний збіг джерел, без змін

```
BurnoutPerTap = 2.2 / FocusStat (базовий Focus = 1)
Пасивне очищення: -1% кожні 25 сек
Поріг: ≥80% → ефективність тапу -85% (лишає 15% ліній)
Кап зниження від бустерів: -25% сумарно
```

## QTE "Баги в коді" — Р3, вирішений блокер 17.1

```
Шанс появи: 3.5% за тап, мін. інтервал 10 тапів
Час реакції: 1.6с база (+до 0.4с від інструментів, макс 2.0с)
Зона спавну: тільки в межах артворку (порада Qwen)
Штраф: -12% від нагороди ЦІЄЇ СПРОБИ за кожен пропуск, адитивно, підлога 0
  payout = base × max(0, 1 - 0.12 × missedCount)
```

## Ремап бустерів — Р4, вирішений блокер 17.6 (таблиця з попереднього документа рішень)

| Стара категорія | Новий сенс на схронах 2-8 |
|---|---|
| Капелюхи/одяг (клік-бонус) | +Focus, −Burnout |
| Генератори (пасив) | Прискорення зняття Burnout |
| Інструменти кліку (множник) | +таймер QTE, +шанс спец-дропу |
| Сила кліку | Далі працює тільки в боях з босами (не міняється) |

## Route-крафт — Р5, прогрес-бар замість миттєвого крафту

`10×paper + 5×intel_data + 2×script → route`, але подається як
`user.routeProgress` (0-100%), не миттєвий crafted-предмет — кожен внесок
ресурсу рухає прогрес-бар. UI: "Маршрут: 32%".

## Стан гравця (нові поля `user`)

```js
activeSprint: null | { tier, startedAt, deadline, linesTotal, linesDone, missedQte, energySpent }
burnout: 0,        // 0-100
focusStat: 1,       // базовий множник, росте від ремапнутих бустерів
routeProgress: 0,    // 0-100, окремо від route-ресурсу (той лишається дропом з вилазки border як і зараз — крафт це ДРУГЕ джерело, не заміна)
```

## Фіче-флаг

`SPRINTS_V2 = false` за замовчуванням (env або константа в `catalog/economy.js`).
Поки вимкнено — гравці на схронах 2-8 грають старим клікером, як зараз.
Новий UI/роути існують, але неактивні. Вмикається окремим рішенням після
Фази 2 (економічний тест).

## Ендпоінти (нові, `routes/sprints.js`)

- `GET /api/sprint` — поточний стан (activeSprint, burnout, focusStat, доступні тіри)
- `POST /api/sprint/start` — `{tier}`, перевірка енергії/рівня схрону, старт
- `POST /api/sprint/tap` — `{clicks}` (як і /api/inspector/hit — серверний anti-cheat батч-вікна), нараховує lines, burnout, шанс QTE
- `POST /api/sprint/qte` — `{hit: bool}` — реакція на QTE-подію
- `POST /api/sprint/claim` — завершення контракту (по дедлайну або по 100% ліній), нарахування ₴ + спец-ресурсів
- `POST /api/sprint/abandon` — дострокове завершення без нагороди

## Що НЕ входить у цей захід

Остаточний тюнінг чисел (Фаза 2 — 3 сценарії: новий/середній/ветеран
гравець, виміряти ₴/год і час до наступного схрону), сама квестова лінія
переїзду схронів (Фаза 3), дерево навичок Тир 2-3 "Стійкість до вигорання"/
"Гострий фокус" активуються тільки після цього заходу.
