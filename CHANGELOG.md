# CHANGELOG

Технічний журнал змін `ukhylyant_simulator` — для розробника. Зворотна
хронологія (найновіше зверху), кожен запис = один коміт (`git log` для
точного diff). Ігровий опис і "просто словами" — `GAME_DESIGN.md` і
опублікований Artifact-огляд; тут — що саме змінилось у коді і чому.

---

## 2026-08-08 — Рівень ухилянта, мінігри, модуляризація

### `00f1f88` `3c9db27` `b625a95` Журнал: позначено Фази 1-3 модуляризації як виконані
Оновлення `CONTINUE_PROMPT.md` після кожної фази — без змін коду.

### `b5c22f5` Modularization Phase 3: extract user model to lib/user-store.js
Винесено: `usersDB`/`clansDB` (Map у пам'яті), дисковий бекап
(`loadData`/`saveData`/`DATA_DIR`/`DATA_FILE`), `createFreshUser`,
`migrateUser`, `installBalanceTracking` (balance-акцесор із `totalEarned`/
`balanceRev`), `migrateLegacyPortfolio`, `displayName`/`nicknameTaken`,
pid-система (`pidIndex`/`makePid`/`registerPid`/`userByPid`).
`getUser()`/`syncHeatAndNotices()` свідомо лишились у `server.js` —
оркестрація моделі+heat+notices, не сама модель. `server.js`: 9593→9230.
Кожну функцію звірено байт-в-байт (`diff` проти `git show HEAD:server.js`)
перед видаленням оригіналу — спіймано і свідомо збережено дублікат ключа
`lastSeenAt` в `createFreshUser` (другий запис перекриває перший,
ефективне значення завжди було `0`, не `Date.now()`) — чистий рефакторинг
не мав права тихцем міняти поведінку.

### `94083ce` Modularization Phase 2: extract pure mechanics functions to lib/mechanics/*.js
`ECONOMY` перенесено в `catalog/economy.js` (відкладено з Фази 1, стало
передумовою для чистих require() у Фазі 2). `lib/utils.js` (`byId`).
`lib/mechanics/skills.js` (`hasSkill`, `applySkillLimits`),
`reputation.js` (`repOf`, `repMaxed`), `heat.js` (`heatTierOf`,
`heatIncomeMult`, `heatRaidMult`, `changeHeat`, `decayHeat` — вимагає
skills.js+reputation.js для `quiet`/`ghost`/Оксана-репутації), `economy.js`
(`UPGRADE_BASE`, `TIER_GATES`, `upgCost`, `upgEffectPerLevel`,
`tierGateCost`, `upgradeGateInfo`, `tierCostMultCapped`), `levels.js`
(`addXP`, `addUkhyr`, `xpForLevel`, `playerLevelForXP`, `ukhyrRank`),
`map.js` (`MAP_BUILDING_BY_ID`, `mapBuildingLevel/Effect`, `mapRaidMult`,
`mapProtectPct`). `server.js`: 10040→9593. `ACHIEVEMENTS` лишився в
`server.js` — його `check()`-замикання читають `clanContributionOf()`/
`SKILL_BY_ID` (рантайм-похідні, не чисті дані).

### `72407f0` Modularization Phase 1: extract pure data catalogs to catalog/*.js
27 констант-каталогів → 16 файлів `catalog/*.js` (SYMPTOMS/INSPECTORS/
TROPHIES, DEFERMENTS/CHECKPOINT_CHOICES/REPUTATION_NPCS/LEAGUES,
SKILL_BRANCHES, SEASON_COSMETICS/COSMETICS, HEAT_TIERS/NOTICE_TYPES,
RESOURCES/CRATES, RECIPES, MAP_BUILDINGS, EXPEDITIONS/PET_EXPEDITION,
LOCATIONS, PETS, QUESTS, ROOM_ITEMS, REVENGE_LINES/PROMO_CODES/
LEGACY_ASSET_PRICES, WHEEL_SEGMENTS/RISK_TIERS/MEMORY_*, UKHYR_RANKS).
Написано власний посимвольний парсер (враховує рядкові літерали й
коментарі — наївний regex зламався б на дужках усередині
ACHIEVEMENTS-замикань) для точних меж блоків. **Мало не запушено в
`data/`** — та назва вже зайнята рантайм-збереженням гри
(`data/gamedata.json`) і повністю в `.gitignore`; перейменовано в
`catalog/` до коміту, інакше наступний деплой впав би з
`MODULE_NOT_FOUND`. `server.js`: 10752→10040. Бекап перед стартом: git tag
`pre-modularization-monolith` (запушено) + знімок `/api/admin/backup` у
`backups/`.

### `ec0a9df` Add global bot.catch() safety net, fix unguarded answerCbQuery in ad-consent flow
`ctx.answerCbQuery()` у флоу згоди на рекламу (`bot.action('ad_consent_yes'
/'ad_consent_no')`) не було обгорнуто в try/catch, на відміну від сусідніх
Telegram API-викликів у тій самій функції — необроблена помилка (протухлий
callback_query) могла впасти без сліду. Додано `bot.catch()` глобально —
жодна необроблена помилка в `bot.on`/`bot.action` більше не валить процес.

### `58da023` Add 3 small minigames: Швидкісна монетка, Колесо ризику 2.0, Знайди пару
Нова вкладка «🎲 Міні-ігри», не пов'язана з основною прогресією (жодного
впливу на XP/Ухирацію/множники доходу):
- **Coinflip** (`POST /api/minigame/coinflip`): ставка 100–20000 ТК
  (`ECONOMY.MINIGAME_STAKE_MIN/MAX`), `COINFLIP_WIN_CHANCE=0.47`, виграш
  ×2 ставки. RNG і валідація ставки — тільки на сервері.
- **Risk wheel** (`POST /api/minigame/risk`): `RISK_TIERS` — safe ×1.5
  (60%), medium ×3 (30%), extreme ×10 (8%); EV кожного ~0.8-0.9, щоб не
  стати новим "золотим рецептом" на кшталт smuggle_kit нижче.
- **Memory match** (`POST /api/minigame/memory/start`+`/flip`): 4 пари
  карток (`MEMORY_ICONS`), вхід `MEMORY_ENTRY_COST=200`. Сервер тримає
  перетасовану колоду в `user.memoryGame` (клієнт бачить значення картки
  лише після її власного фліпу — інакше можна підглянути наперед).
  Нагорода за кількість спроб (`MEMORY_REWARD_TABLE`): ≤6→3000,
  ≤8→2000, ≤12→1000, ≤16→500, більше→200 ТК.

### `55f36f7` Record modularization plan; mark v2.1 level journal as shipped
Записано 5-фазний план розбиття `server.js` у `CONTINUE_PROMPT.md`
(catalog → lib/mechanics → lib/user-store → routes → client HTML/JS),
без коду.

### `40b232c` Fix xp/playerLevel/ukhyr lost on redeploy, missing sync after inspector win
Два реальні баги, знайдені фоновим аудитом одразу після реалізації v2.1:
1. `saveToCloud()` (клієнт) не включав `xp`/`playerLevel`/`ukhyr` у
   CloudStorage-бекап — усі сусідні нові поля (`mapBuildings`,
   `mapPlacements`, `upgTiersUnlocked`) додались, ці три пропущено. Диск
   Render не переживає редеплой, тому після кожного деплою рівень тихо
   скидався на 1, попри те що документований як "ніколи не скидається".
2. `flushInspectorClicks()` не викликав `syncLevel(data)` після перемоги
   над інспектором, хоча сервер там нараховує +200 XP/+15 Ухирації і
   повертає їх у відповіді — тост і розблокування вкладок запізнювались
   до наступного `/api/save`, і то лише якщо ТОЙ save сам давав ненульовий
   приріст XP.

### `292571e` Update in-game FAQ and first-run tutorial for the v2.1 level system
Codex-розділ "basics" досі посилався на видалену `ECONOMY.UPGRADE_GROWTH`
(рендерилось як `undefined`) — переписано під ешелонну формулу. Додано
розділи `levels`/`ukhyr`/`map` у CODEX (карти взагалі не було
задокументовано). `UKHYR_RANKS` використовувався в новому клієнтському
CODEX-розділі, але не був прокинутий у клієнт через
`${JSON.stringify(...)}` (на відміну від решти каталогів) — додано.
Перший туторіал (`help-overlay`) радив новачку відкрити ящики/вилазки —
обидві вкладки тепер сховані до 7/4 рівня; замінено на пояснення самої
рівневої системи.

### `e4c19ed` Add v2.1 "Рівень ухилянта": XP/player-level onboarding gate + Ухирація ranking
`user.playerLevel` (окремий від `user.level`, "рівня схрону") росте від XP,
нарахованого в 10 точках: кліки (+5/100), рівень апгрейду (+8),
пробитий ешелон (+60), крафт (+15), нова локація (+150), вилазка
(+30…+120 лінійно від тривалості), перемога над інспектором (+200),
щоденний квест (+40), пережита облава (+25), легалізація (+500).
`addXP()`/`xpForLevel(l)=round(40·l^1.5)` — той самий стиль, що вже наявні
`changeHeat`/`addWarPoints`. Гейт — клієнтський (`LEVEL_UNLOCKS` у
`buildHtml()`, toggle `.hidden` на `.tab[onclick*="..."]`), НЕ серверний:
нічого прихованого не дає економічної переваги від дострокового
відкриття. Єдиний винятковий серверний гейт — `maybeSpawnInspector()`
(рівень 10), бо інспектори спавняться самі тіком сервера.
`migrateUser()` бекфілить `playerLevel` заднім числом для старих
збережень (`totalClicks`/`craftedCount`/`expeditionsDone`/`raidsSurvived`/
`prestigeCount`/`level`), одноразово (`isPreLevelUser` перевіряє
`playerLevel === undefined`). Паралельна `user.ukhyr` (не валюта) —
рейтинг для `/api/leaderboard`, `ukhyrRank()` з 9 порогів.

### `648a4db` Plan v2.1 "Рівень ухилянта" — journal only
Розібрано й переписано чернетку ChatGPT (спрощено з серверного гейту на
15+ ендпоінтах до клієнтського; виправлено 2 погані місця розблокувань —
крафт за 3 рівнем і квести за 8-м, обидва основні гачки раннього залучення).

### `13731ec` Soften "Легка рука" skill, fix marathon-skill energy display in solo inspector fights
`ECONOMY.SKILL_LIGHTHAND_ENERGY_COST` 1→1.5 (клік коштував 1 енергії
замість 2, повне подвоєння кліків на баку — надто сильно для 3-ї з 6
навичок гілки; тепер +33% замість +100%). "Марафонець" НЕ чіпали — код
явно коментує, що це обов'язковий ключ до боса Генерала Півника (250k HP
за 45с непрохідні з витратою енергії). Знайдено й виправлено попутний баг:
`inspector-hitzone` (сольний бій) не перевіряв `hasSkill('marathon')`
перед оптимістичним списанням енергії на клієнті, на відміну від
`district-hitzone` (кооп-бос) — сервер завжди рахував правильно, баг був
суто в UI-показнику для гравців із цією навичкою.

### `5e10187` Fix 3 critical economy-audit findings
1. `smuggle_kit` (рецепт "Набір контрабандиста"): ресурсний кошт ≈5620 ТК,
   продаж 40000 ТК → безризиковий арбітраж ×7. Знижено до 9000 (маржа
   ×1.6, як у решти крафту).
2. Вилазка `warehouse` ("Нічний склад", 480хв/25% ризику) давала ~7.8
   ТК/хв, а `village` ("Поїздка до баби", 240хв/15% ризику, той самий
   minLevel) — ~8.9 ТК/хв: довша й ризикованіша строго програвала.
   Здобич warehouse піднято на ~40%.
3. `upgCost()` множив на `TIER_COST_MULT^tier` (22^tier) без межі — рівень
   100 (10-й ешелон) коштував би ~10¹⁵ ТК. Додано `tierCostMultCapped()`:
   дзеркалить уже наявний softening `tierGateCost()` (×1.6/ешелон за межею
   5-го), застосовано і до ТК-ціни рівня.

### `648a4db`…назад: fix формула прогрес-бару, recalibrate overlay
`f0456f3` Recalibrate gardrobe overlay position on the character (was floating off-head) — координати `#room-cosmetic-hat/face/neck` підігнано під реальні пропорції нового шару персонажа.

### `ff926d7` Add clean bunker/couch backgrounds, free map building placement, no-store API cache
`room-1-couch.webp`/`room-6-bunker.webp` замінено на чисті фони (без
вбудованого персонажа) — бункер уперше отримав `roomImg` (раніше не мав
жодного, показувалась квадратна `img`-заглушка). `POST /api/map/place`:
`user.mapPlacements` — `{x,y}`% координати іконки будівлі на карті, окремо
від `mapBuildings` (рівень/ефект). Валідація: `mapBuildingLevel > 0`
(нема сенсу розміщувати незбудоване), `0≤x,y≤100`. `app.use('/api', ...
Cache-Control: no-store)` глобально — підозра, що Telegram WebView кешував
GET-відповіді (лідерборд показував застарілий нік).

### `86ab0d1` Add shared transparent character layer for room screen, economy audit doc
Один прозорий PNG персонажа (`room-character.webp`, alpha-обрізаний до
контенту) поверх `.room-scene` замість персонажа, вмальованого в кожен фон
окремо — гардероб тепер позиціонується відносно ЦЬОГО шару, однаково для
всіх локацій. Одразу виправило човен/бункер (раніше взагалі без
персонажа). `ECONOMY_NUMBERS.md` — повний виверт констант гри для
зовнішнього LLM-аудиту балансу.

### `cc52fd6` Scale room-screen gardrobe overlay to container size
`#room-cosmetic-hat/face/neck` мали фіксовані пікселі (52/38px),
розраховані під маленьку кнопку-клікер — на великому `.room-scene` це
було мікроскопічним (десктоп, 1237px) або завеликим (мобільний, 347px).
Переведено на % + `cqw` (container query units).

### `95dcc83` Add numeric energy label above the energy bar
Смуга енергії була без цифри — додано `⚡ Енергія: X/Y`.

### `aa166c4` Fix offline income missing prestige bonus, prestige not resetting echelon tiers
1. `applyOfflineProgress()`: коментар над формулою явно каже "base × heat
   × vip × prestige × clan", код множник `prestigeMultiplier()` не мав —
   ветерани з довідками недоотримували офлайн-дохід пропорційно бонусу.
2. `/api/prestige/claim` скидав `user.upgrades`, але не
   `user.upgTiersUnlocked` — уже пробиті ешелони лишались відкритими
   назавжди після легалізації, обходячи ресурсну плату при повторній
   прокачці тих самих рівнів.

### `0056c49` Аудит: XSS у назві клану, втрата ніка при редеплої, витік реальних імен, gzip
Фоновий агент-аудит, 4 знахідки: (1) `/api/clan/create` без
character-whitelist на `name` + 3 клієнтські `innerHTML`-рендери без
`esc()` — XSS; (2) `nickname`/`pendingNickname` не було в
`saveToCloud()`/`/api/restore` — губились при редеплої; (3) 7 місць
світили `user.name`/`suspect.name` замість `displayName()` — витік
реального імені Telegram у розшуку/офлайн-логах/PvP-повідомленнях;
(4) гонка в потоці оплати зміни ніка — payload тепер несе цільовий нік
напряму (base64), не покладається на мутабельний `pendingNickname` на
момент підтвердження оплати. Заразом: `compression` middleware
(370КБ→85КБ на сторінку).

---

## 2026-08-07 — Редизайн, Вертикаль v2.0, карта території MVP

- `2956b0f` Платна зміна ніка (перша безкоштовна, далі 1000 ⭐) + дисклеймер щоразу при вході
- `c92b255` Компактні вкладки: сітка-плитка замість горизонтального скролу
- `5fe6b11` v2.0 «Вертикаль» Ф1+Ф2: ешелонна система апгрейдів (`TIER_SIZE=10`, `TIER_COST_MULT=22`, `IN_TIER_GROWTH=1.35`, `TIER_EFFECT_MULT=2.2`), приватні ніки, «Наступний крок» (`nextStep()`), дисклеймер
- `5f71a37` Кнопки-іконки з шапки → плитка дій під розшуком
- `5904ddf` Кежуал-редизайн: каштаново-медова палітра (`--bg:#171310`, `--accent:#9c5330`, `--gold:#e0a52e`), Comfortaa/Nunito замість Orbitron/Rajdhani
- `6e4b3bc` `Cache-Control: no-store` на головній сторінці
- `bbb8c37` Теплий прохід по кольорах: шапка й повноекранні оверлеї
- `4e08c62` **Терміновий фікс**: прибрано `tg.requestFullscreen()` — вмикав власний Telegram-хедер, що перехоплював тапи по кнопках гри
- `408e502` Фікс: неможливо було вийти з екрана навичок на телефоні (safe-area padding не рухав абсолютно позиційовану кнопку закриття)
- `80319fc` Картки компаньйонів: градієнт-підклад для читабельності тексту
- `acbb2de` Банери компаньйонів, реальні картинки пса/щура (заміна emoji), окремі вкладки Вилазки/Навички
- `1d71ced` Фікс гонки запитів у боях з інспекторами (флаг відкриття/закриття), fullscreen, тепла палітра
- `9ff94eb` Редизайн головного екрана: клікер окремою вкладкою, статична значка замість персонажа локації на кнопці
- `6b966cd` Карта території MVP: `MAP_BUILDINGS` (вежа/схованка/тайник, 3 рівні кожна), `POST /api/map/build`, ефекти вплетено в `mapRaidMult()`/`applyNoticePenalty()`/`loseRandomResources()`/snitch-крадіжку
- `e12185d` Фаза 1 економічного апдейту: ресурси-будматеріали (wood/scrap/brick), вилазка "Розбір руїн", 3 нові рецепти крафту, підкручені `UPGRADE_GROWTH`/`STORAGE_UPGRADE_GROWTH`/`HEAT_DECAY_DAILY_CAP` (тільки вперед, не заднім числом)

## 2026-08-06 — Захист ендпоінтів, донат-механіки, живі баги

- `e01f930` KATOK видає всі 4 донатних ящики; фікс зламаного посилання wardrobe
- `0b61158` Уламки донатних ящиків (крафтяться в донатні без Stars), промокод KATOK, опт-ін на рекламу (+3000 ТК, глобальний множник аірдропу)
- `9aea342` `GET /api/admin/backup` — позаплатформний бекап гравців/кланів (захищено `x-admin-token`=`BOT_TOKEN`)
- `f1963cf` **3 живі баги після деплою**: клік на 0 енергії давав повний дохід (перевірка була `<=0`, клік коштує 2, енергія рідко рівно 0); чати ОСББ пропадали при редеплої; прогрес губився при відхиленому балансі (клієнт брав серверну цифру, викидаючи наклікане)
- `326487b` Обрізка `clickVal` до серверного значення — тільки для гравців із серверною історією (щоб не збити тих, хто повертається після редеплою з порожньою базою)
- `afbf55e` 3 діри в захисті нових ендпоінтів закрито
- `bc5a5cc` Щоденні квести під нові механіки + фікс старого лічильника
- `717e652` 29 нових досягнень + аудит продуктивності (важкі render-функції прибрано з гарячого `setInterval(100мс)` циклу)
- `3c5d116` Сезони (тижневий скид, київський час), 6 ліг, війни ОСББ, кооп-бос «Облава на район», повна довідка механік (CODEX)

## 2026-08-05 — Розширення 2.0: heat, повістки, боси, PvP

- `28ed3c7` Репутація з районом (4 NPC), полагоджене відновлення прогресу
- `4591dff` QoL: апгрейди пачками ×1/×10/MAX (`upgradeBatchPlan`), формат чисел (К/М/Б/Т), офлайн-звіт
- `65202bc` Дерево навичок ухилянта (18 навичок, 3 гілки, послідовні)
- `1ab091d` Відстрочки (6 типів) та блокпост (3 картки шансу)
- `a5d7467` Медкомісія (карткова міні-гра) та інспектори ТЦК (боси)
- `0837eec` PvP «Здати сусіда» — стук/розслідування/підозрювані
- `76df3f7` Розшук (heat, 6 тірів) і повістки (5 типів) — фундамент розширення 2.0
- `345488b` Досягнення за внески у скарбницю ОСББ
- `1c2f4a8` Клани: скарбниця, рівні (по кореню від скарбниці), спільний бонус
- `f627889` Фікс: ящик за 150 Stars не виконував обіцянку (гарантована косметика)
- `223c981` Щоденна акція на ящик −35% (детерміновано від дати)
- `6b70224` Біржа торгує справжніми ресурсами з кладовки (раніше — абстрактні товари)
- `d910916` Журнал продовження: архітектурні рішення, які не можна ламати
- `9d42fec` +2 рецепти крафту, +2 вилазки
- `be4b2fd` Усі згенеровані картинки задіяно + ресурс «Домашня ковбаса»
- `a87918f` Довідка «Як грати» для новачків
- `f202ff3` Екран статистики та колекції (вкладка ТОП)
- `1a51dc0` 3 компаньйони з бонусами до вилазок, 6 предметів декору кімнати

## 2026-08-04 — Контент: кладовка, крафт, вилазки, престиж

- `7246d38` Престиж «Легалізація» — ендгейм із нескінченною глибиною
- `4173430` Вилазки — офлайн-механіка збору ресурсів за реальний час
- `a892e53` Ресурси, кладовка, крафт, 8 ящиків з триетапною анімацією відкривання
- `ebcab05` Оптимізація картинок: PNG→WebP, ресайз під реальний розмір показу (70МБ→1.4МБ)
- `7b2b953` Екран «Кімната» (персонаж праворуч), донат/промокоди, CloudStorage-бекап прогресу

## 2026-08-03 — Заснування гри

- `3f8fef4` Картинка метелика (bowtie) в гардероб
- `8e96d18` Підключено згенеровані картинки, спрощено вдягання гардеробу, фікс перформанс-багу
- `8dfa875` Промокоди NEVYCHERPNO (нескінченний баланс), OBNULYUVACH (повне обнулення)
- `f2feb76` Екран «Кімната»: превʼю-локація з гардеробом, декор, помста інспектору
- `e476d82` Гардероб +10 предметів у кожен слот (64 замість 24)
- `c257dae` Гардероб розширено до 24 предметів (7 капелюхів, 6 масок, 4 аксесуари, 7 рамок, включно з анімованою веселковою)
- `cfd86bf` Гардероб, квести, нові рівні схрону, розширені досягнення, топ кланів, файлове збереження, неоновий редизайн
- `de30457` `render.yaml` + README з інструкцією деплою на Render.com
- `888ed38` **Перший коміт**: повна базова гра — локації, компаньйони, біржа, клани, gacha, webhook-режим

---

## Умовні позначення

- Хеші — короткі (`git show <hash>` для повного diff і опису).
- Записи 2026-08-08 мають розгорнутий технічний опис (root cause, файли,
  верифікація) — писались одразу як частина коміту. Старіші записи
  стиснуто до суті з підпису коміту.
- Ігрові числа/формули з журналу можуть відрізнятись від поточного стану
  коду, якщо після запису відбулись подальші баланс-правки — звіряй з
  `GAME_DESIGN.md`/`ECONOMY_NUMBERS.md` як джерелом істини "зараз".
