# PATCH 2.0 — Результати аудитів (розраховано на реальних даних коду, 2026-08-13)

Джерело чисел: `catalog/resources.js`, `catalog/recipes.js`, `catalog/economy.js`,
`catalog/expeditions.js`, `lib/mechanics/economy.js`. Усі розрахунки — реальний
виконаний JS-код проти поточного стану репозиторію, не оцінка "на око".

---

## 🔴 Знахідка 1 (нова): провал ціни на кожному переході ешелону, починаючи з 6-го

Функція `tierCostMultCapped()` (`lib/mechanics/economy.js:56`) мала пом'якшити
зростання ціни за межею 5-го ешелону (заміна `22^tier` на `22^5×1.6^(tier-5)`).
Але вона не врахувала, що усередині ешелону ціна ще й множиться на
`IN_TIER_GROWTH^9 ≈ 15` до кінця ешелону (рівень …9 з 10). Результат: **ціна
рівня падає в 9.3 рази щоразу, коли гравець переходить у новий ешелон**,
починаючи з рівня 60, і повторюється на кожному наступному переході (70, 80,
90 … 250+, перевірено скриптом).

```
рівень 59→60:  3 070 275 251 → 329 832 448   (у 9.3× дешевше)
рівень 69→70:  4 912 440 402 → 527 731 917   (у 9.3× дешевше)
рівень 99→100: 20 121 355 887 → 2 161 589 931 (у 9.3× дешевше)
... і так кожні 10 рівнів
```

Перевірено на апгрейді `hat`, формула `upgCost()` спільна для всіх чотирьох
(`hat/jam/thermos/generator`) — баг системний, не поодинокий випадок.

**Причина:** `upgCost(base, level) = base * tierCostMultCapped(tier) * IN_TIER_GROWTH^inTier`.
На останньому рівні ешелону (`inTier=9`) множник `IN_TIER_GROWTH^9≈15` вже
"з'їдений" в ціну. На першому рівні нового ешелону (`inTier=0`) той множник
= 1, а стрибок `tierCostMultCapped` між ешелонами за межею cap=5 — лише ×1.6.
15 vs 1.6 — звідси провал.

**Що можна виправити** (не зроблено, потребує рішення розробника): або
підняти tier-степ множник за межею cap до величини, що компенсує втрату
`IN_TIER_GROWTH^9` (наприклад ×15 замість ×1.6), або перебудувати формулу так,
щоб перший рівень нового ешелону явно продовжував криву попереднього
(`base * tierCostMultCapped(tier-1) * IN_TIER_GROWTH^10`), а не стрибав на
`inTier=0`.

---

## 🔴 Знахідка 2 (нова): «Набір контрабандиста» знову збитковий

Коментар у коді (`catalog/recipes.js:29-32`) каже, що рецепт знижений з
40 000₴ до 9 000₴ виплати, щоб дати маржу ×1.6 замість арбітражу ×7 — і на той
момент це було правдою. Але на **поточних** цінах продажу ресурсів (`fuel`,
`sim`, `cash`, які пізніше піднімали окремо) вартість інгредієнтів зараз:

```
fuel×12 (290₴) = 3 480₴
sim×8   (360₴) = 2 880₴
cash×3  (1250₴) = 3 750₴
Разом: 10 110₴ вартості ресурсів → виплата 9 000₴
```

Тобто зараз це чистий **збиток −1 110₴** за крафт — рецепт випадково зламали
в протилежний бік пізнішою зміною цін ресурсів. Зараз вигідніше продати
fuel/sim/cash напряму, ніж крафтити «Набір контрабандиста».

**Виправлення:** або підняти виплату рецепту (наприклад до ~14 000-16 000₴,
щоб зберегти цільову маржу ×1.4-1.6), або трохи знизити вартість інгредієнтів.

---

## 🟡 Підтверджено живою: ціна Білого Квитка (9000₴/шт) тривіалізує пізню гру

`ticket.sell = 9000` (`catalog/resources.js`). Порахований вплив:

Вилазка **«Нічний візит у ТЦК»** дає **63.5₴/хв** — це у 3+ рази вигідніше за
будь-яку іншу вилазку в грі (наступна краща — «Рейд по смітниках», 21.8₴/хв),
майже виключно завдяки очікуваним ~1.5 квитка за похід × 9000₴.

Водночас рецепт «Справжній Білий Квиток» (постійний імунітет, фінальна ціль
гри) споживає 5 квитків — тобто гравцю економічно вигідніше просто продавати
квитки Толіку, ніж крафтити фінальну мету гри.

```
₴/хв вилазок (за середнім лутом, поточні ціни продажу):
Рейд по смітниках       21.8₴/хв
Нічний склад             19.8₴/хв
Прогулянка до кордону    18.5₴/хв
Розбір руїн              17.3₴/хв
Поїздка до баби в село   16.0₴/хв
Вилазка на ринок          15.5₴/хв
Нічний візит у ТЦК       63.5₴/хв  ← аномалія, у 3× вище за все інше
```

**Виправлення (потребує рішення розробника, не зроблено):** знизити
`ticket.sell` (наприклад до ~2 500₴, як пропонував Грок у обговореннях) або
прибрати можливість продажу квитків напряму й лишити їх тільки крафтовим
ресурсом.

---

## ✅ Закрито / не є проблемою (не потребує дій)

### `UPGRADE_GROWTH=1.58`, рівень 40 = 194 млрд ТК
Ця константа **більше не існує в коді**. Її замінила ешелонна система
(`TIER_SIZE=10`, `TIER_COST_MULT=22`, `IN_TIER_GROWTH=1.35`,
`TIER_EFFECT_MULT=2.2`, `catalog/economy.js:12-15`) ще до цієї сесії. Стара
цифра з документів обговорення застаріла — ігнорувати весь пункт Р7 у
попередніх файлах у тому вигляді, як він сформульований.

### «Село до баби» vs «Нічний склад»
Вже виправлено раніше цієї сесії: склад тепер **19.8₴/хв** проти **16.0₴/хв**
у баби. Довший і ризикованіший маршрут коректно вигідніший за коротший.

### EV ящиків за ТК (усі 4 типи)
```
Картонна коробка (400₴):        EV/ціна = 0.34
Гуманітарний ящик (2500₴):       EV/ціна = 0.19
Посилка від родичів (9000₴):     EV/ціна = 0.25
Контрабандний контейнер (60000₴): EV/ціна = 0.13
```
Усі суттєво нижче 1.0 — ящики НЕ друкують гроші, навпаки — доволі слабка
віддача гравцю. Дірки «нескінченних грошей» з ящиків немає, попри те, що
питання про це піднімалось в обговореннях.

---

## 📋 Не порахувати з коду (потребують окремого підходу, не аудиту-обчислення)

- **Route-craft EV vs старий RNG (17.3)** — у коді досі немає ресурсів
  `script/intel_data/crypto_key` (Sprint-система ще не реалізована), тому це
  можна тільки оцінити на папері з чужих джерел, не порахувати з поточного
  коду.
- **Item-портфоліо (17.12)** — не обчислення, а документаційний прохід по
  всіх предметах усіх каталогів. Розробник назвав це одним з найважливіших
  завдань — варто окремим заходом, не змішувати з цим блоком.
- **Термінологічний словник (17.4)** — так само документаційна задача
  (мапінг жаргону з обговорень типу «shard» на реальні ID ресурсів у коді).

---

## Підсумок пріоритетів

| # | Знахідка | Критичність | Статус |
|---|----------|-------------|--------|
| 1 | Провал ціни ешелону ×9.3 (рівні 60+) | 🔴 Висока — реально ламає прогресію на пізніх рівнях | Знайдено цим аудитом, не виправлено |
| 2 | `smuggle_kit` збитковий (−1110₴/крафт) | 🔴 Середня-висока — мертвий рецепт | Знайдено цим аудитом, не виправлено |
| 3 | `ticket.sell=9000` — tcc_office ×3 вигідніше за все | 🟡 Середня — відомо раніше, тепер підтверджено цифрами | Не виправлено |
| — | `UPGRADE_GROWTH=1.58`/рівень 40 | — | Застаріло, константи нема в коді |
| — | Село vs Склад | — | Вже виправлено раніше |
| — | EV ящиків | — | Перевірено, проблеми нема |
