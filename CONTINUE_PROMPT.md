# Продовження проєкту ukhylyant_simulator

Працюю над "Симулятор Ухилянта" — сатиричний Telegram Mini App клікер для друзів
(Node.js + Express + Telegraf, весь бекенд і фронтенд в одному файлі `server.js`).

## Де що лежить

- Проєкт: `C:\Users\User\Desktop\Архів скриптів\Клод-код\ukhylyant_simulator`
- GitHub: https://github.com/patyjprolot-ux/ukhylyant-simulator (гілка `master`)
- Деплой: Render.com, https://ukhylyant-simulator.onrender.com (Blueprint, автодеплой на push)
- Бот: @yhuliannt_bot (реальний BOT_TOKEN в `.env`, той самий що й на Render)

## ⚠️ КРИТИЧНО перед будь-яким локальним запуском

`server.js` в режимі `USE_WEBHOOK=false` викликає `bot.telegram.deleteWebhook()` —
а це ТОЙ САМИЙ токен, що працює в проді на Render. Якщо запустиш локально без
обережності — знімеш вебхук з живого бота і він перестане відповідати друзям.

Правильний спосіб локального тесту (безпечний, не чіпає прод):
```bash
ALLOW_UNVERIFIED_DEV=true WEB_APP_URL=https://ukhylyant-simulator.onrender.com node server.js
```
Це лишає `USE_WEBHOOK=true` (дефолт) і реєструє webhook на ту саму продакшн-адресу
(нешкідливо, бо публічно доступний тільки Render). Відкривати гру локально —
`http://localhost:3000`, `ALLOW_UNVERIFIED_DEV=true` дозволяє тестувати без
реального Telegram initData.

Після тесту — обов'язково зупинити локальний процес (`taskkill /F /PID ...`) і
видалити `data/` (тестові дані, не мають потрапити в git — і так у `.gitignore`).

## Поточний стан гри

**Повний опис усіх механік — у `GAME_DESIGN.md`** (тримай його оновленим).

Локації 1-6, магазин багаторівневих апгрейдів, 8 ящиків з анімацією відкривання,
кладовка з 11 ресурсами, крафт (10 рецептів), вилазки (6), престиж «Легалізація»,
компаньйони (6), Колесо Зради, щоденний Пайок, досягнення (37+), щоденні квести,
тіньова біржа, клани, реферали, VIP і донат за Stars, промокоди, статистика й
колекція, довідка «Як грати».

### Розширення 2.0 — план на 6 фаз

`EXPANSION_2.0_PLAN.md` — узгоджений план великого розширення (heat, повістки,
медкомісія, боси-інспектори, відстрочки, PvP-стук, сезони/ліги, війни кланів,
дерево навичок, репутація з NPC, QoL). Там же — статус фаз і балансні
виправлення, які треба тримати в голові. **Розширення 2.0 виконане повністю
(усі 6 фаз).** Понад план додано довідку механік.

- **Довідка механік (`CODEX` у клієнтському JS)** генерує числа з реальних
  даних гри, а не дублює їх текстом. Додаєш нову механіку — додай і розділ,
  але НЕ вписуй цифри руками: бери їх з `ECONOMY` і масивів. Каталоги для
  цього пробрасуються в клієнт у `buildHtml()` поруч із `NOTICE_TYPES`.
- **Тижневі механіки (сезон, війни, «Ухилянт тижня») живуть у 5-хвилинному
  тіку** й дублюються в `/api/user` — процес міг стояти без запитів через
  межу тижня. Межа сезону — `seasonIdFor()`, понеділок за київським часом.
- **Підсумки сезону рахуються ГЛОБАЛЬНО один раз** (`rolloverSeasonIfNeeded`),
  бо ранги залежать від усіх гравців. Групи по лігах треба рахувати ДО
  переміщень — інакше підвищений у лізі 0 потрапляє в ітерацію ліги 1 і
  підвищується знову, до самого бункера. На цьому вже ловились.
- **`clickVal` і `passive` тепер обмежені серверним значенням** у `/api/save`:
  клієнтське приймається, лише якщо не БІЛЬШЕ за те, що сервер колись видав
  (усі легітимні джерела — апгрейди, крафт, престиж, restore — серверні).
  Без цього підроблений `clickVal` у save давав миттєву перемогу над Генералом
  Півником разом із Білим Квитком. Робиш нову механіку, де серверна нагорода
  залежить від клієнтського числа, — обмежуй так само.
- **Вікно анти-чіту в боях рахує сервер** (`serverBatchWindow`), а не `dt` із
  тіла запиту: інакше `dt=5000` кожні 500мс давав удесятеро більший урон.
- **Довідники `*_BY_ID` створюються через `byId()` з null-прототипом.** Ключі
  приходять із тіла запиту, і на звичайному об'єкті `CATALOG['constructor']`
  повертав би функцію з `Object.prototype` — код падав у 500. Новий довідник
  роби так само.
- **Dev-роути для тижневих механік** (лише при `ALLOW_UNVERIFIED_DEV`):
  `/api/dev/end-season`, `/api/dev/start-war`, `/api/dev/end-war`,
  `/api/dev/spawn-district`, `/api/dev/spawn-inspector`.

- **Новий прогрес треба додавати у списки відновлення** (`RESTORE_NUMBER_FIELDS`,
  `RESTORE_ARRAY_FIELDS`, `RESTORE_MAP_FIELDS`) і в `saveToCloud()` на клієнті.
  Інакше відновлення з CloudStorage після редеплою Render мовчки поверне
  гравця без цього поля. Уже один раз ловились: репутація, навички й трофеї
  спершу туди не потрапили.
- **Вилазки — це масив `user.expeditions`**, не об'єкт (навичка «Друга нора»
  дає другий слот). Старий формат читається міграцією; `expeditionSnapshot()`
  віддає ще й поле `expedition` для сумісності.
- **Бонуси до `maxEnergy` накладай ДЕЛЬТОЮ.** Крафтові бонуси зашиті прямо в
  число, тому будь-який перерахунок «від локації» їх мовчки зітре назавжди.
  Приклад правильного підходу — `applySkillLimits()` зі `skillEnergyBonus`.
- **Бонуси до здобичі не мають діяти на КУПЛЕНЕ.** `addResource()` приймає
  `{ bonus: false }` — саме так біржова купівля не перетворюється на
  нескінченні гроші через цикл купив-продав.
- **Приріст heat заморожений під відстрочкою** (`changeHeat` повертає 0 на
  плюсовій дельті). Це навмисно — див. GAME_DESIGN. Не «чини».
- **Спавн босів і генерація повісток — в одному 5-хвилинному тіку**
  (`tickNotices`). Нові періодичні механіки чіпляй туди ж, не заводь ще один
  інтервал по всіх гравцях.
- **Уважно з `...storageSnapshot(user)` у відповідях.** У нього є поле `used`
  (зайнято місць у кладовці) — власне поле з такою назвою воно затре. На цьому
  вже один раз зловились у медкомісії.
- **Dev-роут `/api/dev/spawn-inspector`** реєструється лише при
  `ALLOW_UNVERIFIED_DEV=true` — у проді його не існує. Зручно для тестів босів,
  далі знадобиться для кооп-боса Фази 5.

- **PvP адресує гравців через `user.pid`, а не Telegram id.** Лідерборд —
  публічний неавторизований ендпоінт, тому чужі Telegram-айді туди не віддаємо.
  Новий гравець отримує pid у `migrateUser`; усі профілі проганяються через
  міграцію одразу після `loadData()`, інакше pid тих, хто ще не заходив у цьому
  процесі, просто не буде в індексі.
- **Механіки, що міняють баланс ЧУЖОГО гравця** (крадіжка при розкритому стуку),
  зобов'язані віддати жертві подію рівно один раз (`pendingRobbery` → `robbery`
  у відповіді `/api/save`). Інакше вона побачить лише тихий відкат цифри й
  вирішить, що це баг.
- **Чужі ніки — довільний текст.** У розмітку йдуть тільки через `esc()`, а в
  inline-`onclick` — ніколи (лише індекс або серверний pid): апостроф у нікові
  зламав би обробник.

### Ключові архітектурні рішення (не зламай)

- **Енергія — головний обмежувач темпу.** Регенерація ~1/сек
  (`ECONOMY.ENERGY_REGEN_PER_TICK = 0.1` за тік у 100мс). Колись було 20/сек, через
  що енергія взагалі нічого не обмежувала. Не «полагодь» це назад.
- **`user.balance` — акцесор** (`installBalanceTracking`). Рахує `balanceRev`
  (захист від затирання серверних нарахувань клієнтським автозбереженням) і
  `totalEarned` (для престижу). Тому баланс НЕ можна відновлювати з бекапу
  простим присвоєнням — див. коментар у `/api/restore`.
- **Компаньйон фіксується в момент старту вилазки** (`expedition.petId`), інакше
  можна вирушити зі щуром (низький ризик) і забрати здобич із псом (+40%).
- **Анімація ящика має гарантований фінальний стан** (`.anim-done` + таймер на
  2100мс): CSS-анімації не просуваються у згорнутій вкладці й зависають на 0%
  кейфреймі, через що приз лишався невидимим.
- **`migrateUser`** дописує нові поля старим збереженням. Додаєш поле в
  `createFreshUser` — воно автоматично зʼявиться у старих гравців.
- **Не клич важкі рендери в циклі 100мс** — тільки в `init()` і після дій
  користувача (див. `renderOwnedStuff`). Таймер вилазки — окремий інтервал раз
  на секунду і лише коли вкладка видима. Те саме для таймерів повісток: у
  секундному інтервалі оновлюється лише ТЕКСТ таймерів, `renderNotices()` там
  викликати не можна (він перебудовує розмітку з кнопками під пальцем гравця).
- **Розшук (heat) міняється тільки через `changeHeat(user, delta, reason)`** —
  одна точка входу, бо там і кап 0..100, і множник компаньйона-пліткарки, і
  запис у `heatLog` для екрана «Твоя справа».
- **Спад heat і протухання повісток — ліниві** (`syncHeatAndNotices` у
  `getUser`), а не окремий таймер на кожного гравця. Таймер лишився один
  (`tickNotices`, раз на 5 хв) — він видає нові повістки й шле пуші.
- **Множники доходу застосовуються в одному порядку**:
  `base * heat * vip * prestige * clan`. Додаєш новий множник — вписуй у цей
  ланцюг і на клієнті (клік + пасив), і на сервері (офлайн, монети з ящиків).
- **`user.energy` на сервері — це останнє збережене клієнтом значення** (клікер
  клієнт-авторитетний). Механіки, що витрачають енергію серверно (спосіб
  «Сховатись»), працюють із цим значенням і повертають нове клієнту у відповіді.

**Гардероб (COSMETICS)** — слоти hat/face/neck/frame, суто косметика (не впливає
на економіку). Купівля предмета одразу його вдягає (без окремого кроку "екіпірувати").
Рамки (frame) — чистий CSS glow, дві анімовані (rainbow, siren) — анімація
вмикається коротким спалахом лише під час кліку (`pulseFrame()`), не крутиться
постійно.

**Кімната** — окремий повноекранний вигляд (кнопка-«повістка» 📜 в шапці), де
показано широку картинку локації (`roomImg`, 16:9) з персонажем анфас у правій
третині кадру, оверлеї гардеробу зверху в фіксованих координатах, і декор кімнати
(ROOM_ITEMS — 14 речей, можна тримати кілька одночасно).
Контейнер має `aspect-ratio: 16/9` — це критично, бо координати гардеробу
рахуються у відсотках і при іншій пропорції все зʼїжджає.

**Промокоди**: `MAMYN_SYNOK` (VIP), `TISA_SWIMMER` (+5000), `FREE_STARS` (+10000),
`NEVYCHERPNO` (нескінченний баланс), `OBNULYUVACH` (повне обнулення прогресу).

**Збереження прогресу**: `data/gamedata.json` + резервна копія в Telegram
CloudStorage (`save_v1`). Диск Render не переживає редеплой, але CloudStorage
переживає — при вході «свіжий» гравець автоматично відновлюється з бекапу.
Все одно: **не пуш часто дрібними комітами**, групуй зміни.

## Картинки

Усі картинки — **WebP** (сконвертовані з PNG і зменшені під реальний розмір показу:
70 МБ → 1.4 МБ). Нові теж конвертуй, інакше гра знову почне довго вантажитись.
Жодної невикористаної картинки в `public/images/` зараз немає.

**Ще на emoji (немає картинки)**:
- Гардероб (hat): `sock_hat` (🧦)
- Компаньйони: `dog` (🐕), `rat` (🐀), `pigeon` (🕊️)
- Декор кімнати: майже весь (є лише `rug`)
- Ресурси: усі, крім консервів/батарейок/ковбаси
- Іконка входу в кімнату (📜 "повістка")
- **Кімнати (`roomImg`) для човна (Lvl4) і бункера (Lvl6)** — решта 4 є.
  Човен був згенерований у костюмі (мало бути касуально), бункер — персонаж
  сидить (має стояти праворуч). Канон за рішенням користувача: касуальний
  вигляд на рівнях 1-4, костюм на 5-6.

⚠️ `icecube` і `toiletpaper` мають картинки, але на них намальована ціла
голова-манекен — при накладенні на персонажа дублюється обличчя. Варто
перегенерувати як ізольовану іконку.

Готові промпти (стиль вже вписаний у кожен, просто копіювати) — у файлах
`image_prompts.md` (повна історія) та `image_prompts_todo.md` (тільки те, що
не зроблено). Якщо користувач скидає нові картинки з Downloads — файли
називаються `Use AI Image <дата>, <час>.png`, треба відкрити й розпізнати
що на них, скопіювати в `public/images/` з осмисленою назвою
(`cosmetic-<slot>-<id>.webp`, `room-<id>.webp`, `summons-icon.webp`), додати
`img:` поле у відповідний запис масиву в `server.js`, і — важливо — код уже
вміє показувати `img` замість emoji автоматично (в `renderCosmetics()` та
`renderCharacterOverlay()`/`setOverlay()`), додаткових змін логіки не треба,
тільки дані.

## Важливий перформанс-урок (не повторюй цю помилку)

Ігровий цикл `setInterval(..., 100)` викликає `updateUI()` 10 разів/сек.
`updateUI()` НЕ повинен викликати важкі рендер-функції (`renderCosmetics()`,
`renderPets()`, `renderRoomItemsOverlay()`, `applyCosmeticOverlay()`) —
раніше так було, і це перебудовувало HTML під 60+ карток гардеробу щосекунди
кількадесят разів, гра тупила на телефонах. Тепер є `renderOwnedStuff()` —
викликається лише в `init()` і після конкретних дій (купівля/екіпірування),
НЕ в гарячому циклі. Тримайся цього паттерну для будь-яких нових фіч.

## Фаза 1 економічного апдейту — ВИКОНАНО (2026-08-07)

Аудит економіки/складності + 3 нові будматеріальні ресурси (`wood`/`scrap`/
`brick`) + нова вилазка `ruins` + 3 нові рецепти (`reinforced_hideout`/
`scrap_generator`/`brick_wall`) + підкрутка складності (`UPGRADE_GROWTH`
1.55→1.58, `STORAGE_UPGRADE_GROWTH` 1.6→1.68, `HEAT_DECAY_DAILY_CAP` 60→42).
Деталі — `GAME_DESIGN.md`. Усе перевірено локально через прямі виклики API
(крафт/вилазка/дроп із ящиків/апгрейд кладовки) — сервер зупинено, `data/`
видалено. Ще не запушено — коміт зроблено, пуш чекає підтвердження.

## Карта території — MVP ВИКОНАНО (2026-08-07)

Екран 🗺️ у шапці: мальована карта міста (`public/images/map-city-bg.webp`,
ізометричний стиль — навмисно ІНШИЙ за решту гри, узгоджене рішення) з 5
клікабельними орієнтирами, що ведуть у вкладку Вилазки (`jumpToExpedition`),
і 3 захисні споруди (`MAP_BUILDINGS` у `server.js`) — вежа/схованка/тайник,
кожна з 3 рівнями за wood/scrap/brick. Ефекти реальні (не заглушки):
`mapRaidMult()` на клієнті (шанс облави), `applyNoticePenalty` і блокпост/
крадіжка на сервері (`mapBuildingEffect`, `mapProtectPct`). Деталі — розділ
"🗺️ Карта території" у `GAME_DESIGN.md`.

**Свідоме спрощення, яке варто знати перед подальшим розширенням:** будівлі
НЕ прив'язані до конкретної клітинки сітки — будуються як звичайні рівні
апгрейдів (як крафт), сітка на фоні лишається візуальним контекстом і
майданчиком для орієнтирів. Вільне розміщення по клітинках — можливе
розширення пізніше, але не зроблене зараз (не вигадуй, що воно вже є).

Hotspot-координати орієнтирів (`top`/`left` у %) підібрані візуально й можуть
бути неточними на реальному фоні — якщо орієнтир клікається повз позначку на
картинці, підправ координати в `.map-hotspot` (розмітка екрана `map-screen`).

Актив `map-empty-plot.webp` підготовлений, але поки НЕ використовується в
коді (лишився про запас на випадок вільного розміщення в майбутньому).

## Редизайн головного екрана — ВИКОНАНО (2026-08-07)

Клікер тепер ОКРЕМА перша вкладка ("👆 Клікер"), не завжди видимий `<main>`
над вкладками — звільнило місце на екрані. Енергія й розшук (heat-wrap)
переїхали з шапки всередину цієї вкладки. Шапка лишила тільки
баланс/пасив/⭐ і кнопки-іконки (кімната/карта/довідка/повістки/пайок).

**Кнопка-клікер більше НЕ показує персонажа локації** — тепер це одна
статична значка на всіх 6 локаціях (`public/images/clicker-badge.webp`,
"ТЦК — теоретично це кнопка"), однакова незалежно від рівня схрону. Гардероб
(hat/face/neck) відповідно перестав рендеритись на клікері
(`applyCosmeticOverlay()` тепер працює тільки для екрана "Кімната").
**Це свідоме рішення користувача:** кастомізація персонажа — це тепер
виключно прерогатива "Кімнати" (саме там буде сенс переробки кастомізації,
описаної нижче), а не головної кнопки кліку.

## v3.0 «Переправа» — журнал є, відповіді на відкриті питання зафіксовано (2026-08-07)

Користувач передав повний журнал переробки v2.0 «Вертикаль» (ешелони апгрейдів,
престиж ×1.12^n, зв'язування механік) і v3.0 «Переправа» (другий поверх гри:
точки виведення, бюро, другий престиж, антимонопольні запобіжники, клановий
конфлікт за маршрути). **v3 — окремий великий захід ПІСЛЯ того, як v2
стане на ноги** (сам журнал це підтверджує: "Мінімальний іграбельний зріз:
Ф8+Ф9", продовжує нумерацію фаз з v2 Ф1-Ф7). Зараз (ця сесія) — тільки v2
Ф1+Ф2.

Відповіді користувача на 5 відкритих питань §26 журналу v3 (для коли дійде
черга):
1. **Такт рейсу = 1 доба** (не місяць) — підтверджено, як і запропоновано.
2. **Розподіл заявок між точками — повзунок вручну**, не автоматика.
3. **Бюро — окрема вкладка всередині екрана країни**, не окремий екран.
4. **Частота диверсій — на розсуд розробника** (не вирішено конкретно, лишено гнучким).
5. **Ротація маршрутів — усереднити константу** між 3 і 5 із 15 (не точно 3,
   не агресивні 4-5 — щось середнє, підібрати за грою).

Сам журнал v3 лежить поза репозиторієм (Claude Desktop outputs), користувач
може скинути повторно, якщо треба.

**v3.1 — фікс залежності від онлайну (2026-08-07), теж отримано.** Головне
правило на майбутнє для ВСІХ нових механік, не тільки v3: **дохід = PvE-база
+ множник за живих гравців, ніколи "тільки з живих"**. Розрахунковий орієнтир
— 2-3 активні гравці на добу, не 12. Ключові фікси з журналу: NPC-бригади на
маршрутах (заповнюють маршрут, коли живих мало), NPC-опонент для війни ОСББ
без пари, HP облав фіксується через 30 хв від РЕАЛЬНО присутніх (не завищена
підлога), консервація прогресу при 7+ днях офлайн. Контрольний тест перед
пушем будь-якої нової кооп-механіки: "чат — 1 активний гравець, решта зникли
на місяць — що ламається?" Стосується і вже готового Розшуку/Облави на район
(v2) — підлогу 150 000 варто буде переглянути тим самим фіксом (Ф-4), коли
дійде черга.

## v2.1 «Рівень ухилянта» — журнал готовий до реалізації ОДНІЄЮ сесією (2026-08-07)

Користувач передав чернетку від ChatGPT (XP-рівні 1-50 + рейтинг «Ухирація»),
я її розібрав і **свідомо спростив та переробив** нижче — нижче ЄДИНЕ джерело
істини для реалізації, чернетку саму по собі НЕ використовувати як специфікацію
(у ній є застарілі й нездійсненні місця, виправлення позначені явно).

**Мета користувача:** гра стає складнішою (більше цифр/механік), але
зрозумілішою — новачок не бачить одразу ВСЕ (клани/карту/навички/боси), а
відкриває поступово. Явно підтверджено: рівень — **НОВА окрема цифра, ширша
за рівень локації** (1-6 лишається як є, це "рівень схрону"), не заміна.

### Чому спрощую підхід ChatGPT (ключове архітектурне рішення)

Чернетка пропонувала гейтити 15+ систем (магазин/крафт/heat/квести/навички/
інспектори/карту/клан/сезони/PvP) через СЕРВЕРНУ перевірку рівня в кожному
ендпоінті — це велика і ризикована робота (кожен ендпоінт — окрема точка
відмови, легко щось забути й зламати живим гравцям).

**Натомість:** новий рівень — це переважно **клієнтський UI-гейт**
(вкладка/кнопка просто не рендериться, поки рівень не досягнуто), а не
економічний. Причина: нічого з переліченого й так не дає безкоштовної
переваги, якщо відкрити його раніше (це вже сплачені механіки, не халява) —
тому це задача "не перевантажити новачка інформацією", не "запобігти чіту".
Мінімальний сервер-сайд контроль лишень там, де це дійсно щось захищає
(навички: вже гейтяться довідками легалізації, нічого міняти не треба).

### 1. Джерела XP — прив'язані до ВЖЕ ІСНУЮЧИХ лічильників (не нові системи)

Замість вигаданих у чернетці дій ("подвійні ресурси", "крейсер" — таких
механік у грі немає), XP нараховується в ТОЧКАХ, де лічильники вже й так
збільшуються (`totalClicks`, `craftedCount`, `expeditionsDone`,
`raidsSurvived`, `tradesCount`, `boxesOpened`, `prestigeCount`, а також нові
події — перемога над інспектором, виконаний квест, нова локація):

| Дія | XP | Де в коді додати виклик |
|---|---|---|
| 100 кліків (сумарно) | +5 | `/api/save`, там де вже рахується `clickDelta` |
| Рівень апгрейду | +8 | `/api/upgrade/buy`, за кожен рівень у `levelsBought` |
| Пробитий ешелон | +60 | `/api/upgrade/breakTier` |
| Крафт (будь-який рецепт) | +15 | `/api/craft`, при успіху |
| Нова локація | +150 | `/api/save`, де вже ловиться `level > user.level` |
| Завершена вилазка | +30..+120 (за `minutes` вилазки, лінійно) | `/api/expedition/claim` |
| Перемога над інспектором | +200 | там, де `user.inspectorStats.defeated[id]++` |
| Виконаний щоденний квест | +40 | `/api/quest/claim` (чи як він зветься) |
| Пережита облава | +25 | `/api/save`, де вже ловиться `raidDelta` |
| Легалізація (престиж) | +500 | `/api/prestige/claim` |

Формула рівня: `XP_до_наступного(l) = round(40 * l^1.5)` (як у чернетці,
компроміс — не занадто плоско, не занадто круто). Функція `addXP(user,
amount)` — нова, у стилі вже наявних `changeHeat`/`addWarPoints`: інкрементить
`user.xp`, у циклі перевіряє й піднімає `user.playerLevel`, повертає
`{ levelsGained, newLevel }` для UI-тосту "Рівень N!".

### 2. Таблиця розблокувань — ВИПРАВЛЕНА послідовність

Чернетка ChatGPT ховала крафт до 3 рівня і щоденні квести до 8-го — обидва є
основним геймплейним гачком з перших хвилин, ховати їх шкідливо для
утримання гравця. Нижче — виправлений порядок (тільки клієнтський UI-гейт,
таб/кнопка прихована, поки рівень не досягнуто):

| Рівень | Відкривається |
|---|---|
| 1 | Клікер, Магазин апгрейдів, Крафт, Еволюція локацій (усе, що є зараз "з коробки") |
| 2 | Щоденні квести |
| 3 | Кладовка (вкладка, хоча рівень 1 уже мовчки працює) |
| 4 | Вилазки ("Вилазки"-таб) |
| 5 | Розшук/Повістки видимі явно (heat-wrap; технічно й так рахується, але без цифр на екрані новачок про нього не знає) |
| 6 | Біржа |
| 7 | Ящики (гача) |
| 8 | Навички (таб; реальний гейт лишається — довідки легалізації) |
| 10 | Інспектори (боси) |
| 12 | Карта території |
| 14 | Клан |
| 16 | PvP «Здати сусіда» |
| 18 | Сезони/Ліги |
| 20 | Війна ОСББ |

**Existing users (важливо, non-retroactive):** живим гравцям при міграції
рахуємо `playerLevel` ЗАДНІМ ЧИСЛОМ від їхніх наявних лічильників (грубий
підрахунок XP з `totalClicks`/`craftedCount`/`expeditionsDone`/`prestigeCount`
і т.д. за формулами вище, одноразово в `migrateUser()`), щоб досвідчений
гравець не побачив раптом заблоковані вкладки, якими вже давно користується.

### 3. Ухирація (рейтинг, НЕ валюта) — спрощена версія

Окреме поле `user.ukhyr` (число), нараховується в ТИХ САМИХ точках коду, що
й XP вище (просто другий виклик поруч), за таблицею з чернетки (кліки/1000ТК
+1, вилазка +5, інспектор +15, квест +3, облава +10, локація +50, Білий
Квиток +200, легалізація +1000). **Спрощення проти чернетки:** тільки
ОДИН лідерборд (all-time, той самий екран що й зараз `/api/leaderboard`, ще
одна колонка) — денний/тижневий з окремими обнуленнями відкладаємо (це,
по суті, ще одна сезонна система, а вона вже є — `seasonPoints`/ліги).
Ранги/титули — таблиця з чернетки годиться як є (пороги 0/500/1500/4000/
9000/18000/35000/60000/100000), показувати як текстовий бейдж біля ніка.

### 4. Технічний план (для сесії реалізації)

1. `createFreshUser`: `xp: 0, playerLevel: 1, ukhyr: 0`.
2. `RESTORE_NUMBER_FIELDS`: додати `xp, playerLevel, ukhyr`.
3. `migrateUser()`: одноразовий заднячисловий підрахунок для існуючих (див. §2).
4. Функції `addXP(user, amount)`, `addUkhyr(user, amount)`, `XP_FOR_LEVEL(l)`,
   `UKHYR_RANK(points)` — поруч із `changeHeat`/`addWarPoints`.
5. Викликати в 10 точках з таблиці §1 (просто додатковий рядок коду в вже
   існуючих ендпоінтах, не нові роути).
6. `/api/user` і `/api/save` — повертати `xp, playerLevel, ukhyrRank`.
7. Клієнт: `state.playerLevel`, функція `levelGate(n)` → true/false, обгорнути
   рендер вкладок/кнопок у шапці й тайлах перевіркою (за зразком вже наявного
   `hidden`-класу для прихованих елементів). Тост "🎉 Рівень N! Відкрито: …"
   при підвищенні (є вже готовий патерн `unlockedAchievements`-тосту, скопіювати).
8. `/api/leaderboard`: додати `ukhyr` і `ukhyrRank` у відповідь, клієнт — рядок.
9. `GAME_DESIGN.md`: новий розділ.

### Верифікація перед пушем
- `node -c server.js`.
- Локальний запуск, curl-тест: новий гравець → перевірити `playerLevel:1`,
  зробити кілька дій (клік/save, craft, upgrade buy) → xp росте, рівень
  піднімається на порозі.
- Мігрований "старий" гравець (задати великі totalClicks/craftedCount через
  `/api/restore`) → після рестарту сервера (де `migrateUser` спрацьовує)
  переконатись, що `playerLevel` порахований заднім числом, а не 1.
- Браузер: відкрити гру свіжим гравцем, переконатись що вкладки з таблиці §2
  дійсно приховані/з'являються по рівнях.

## Робочий процес

1. Читай `server.js` (єдиний файл, ~2000+ рядків: бекенд-роути зверху,
   `buildHtml()` з інлайн HTML/CSS/JS знизу).
2. Роби зміни через Edit.
3. `node -c server.js` — синтаксис-чек.
4. Локальний тест через безпечний запуск вище + Browser pane
   (`preview_start` → `http://localhost:3000`) + `javascript_tool` для
   прямих перевірок стану/API без реального Telegram.
5. Зупини локальний сервер, видали `data/`.
6. Комміт + push (група змін, не щоразу).

Продовжуй з того місця, де я зупинився — спитай користувача, що робити далі
(нові картинки? нові фічі? щось виправити?), не вигадуй нову роботу сам.
