# Звукові ефекти — список для генерації

Код уже готовий (`playSound(name)` у `server.js`, викликається у всіх
місцях нижче). Файлів фізично ще нема — поки їх немає, гра просто грає
без звуку в цих місцях (`playSound()` тихо ігнорує відсутній файл, нічого
не ламається). Клади готові файли в `public/sounds/` під точними іменами
нижче — жодних змін коду більше не треба, підхопиться само.

Формат: **mp3**, короткі (0.1–1.5с для UI-звуків, до ~3с для великих
моментів), без тиші на початку/в кінці (щоб не було затримки при
швидких повторних тапах — клікер, QTE).

Загальний тон гри: сатиричний, трохи абсурдний, "дешевий мобільний
клікер" — не серйозний AAA-саунддизайн. Приклад орієнтиру: Hamster
Kombat / Township-стиль звуків.

## Список файлів

| Файл | Коли грає | Опис звуку |
|---|---|---|
| `click.mp3` | Кожен клік по персонажу (тільки схрон 1, старий клікер) | Коротке "тік"/монетний дзвін, 0.1-0.2с. Найчастіший звук у грі — має НЕ дратувати при спамі. |
| `qte_hit.mp3` | Влучний тап у QTE (Спринти "Баги в коді" + медична здача аналізів) | Короткий приємний "дінь" або "поп", позитивний, 0.15-0.3с. |
| `qte_miss.mp3` | Невлучний/пізній тап у тому самому QTE | Коротке глухе "бз" або "тьомп", не різке, 0.15-0.3с. |
| `upgrade.mp3` | Купівля рівня апгрейду в магазині | Механічний "клац" або коротке зростаюче "віп", 0.3-0.5с. |
| `craft.mp3` | Успішний крафт предмета АБО хвороби | Коротке "готово" — дзвіночок або легкий сплеск, 0.3-0.6с. |
| `crate_open.mp3` | Розкриття ящика — звичайний приз | Коротка весела мелодійна фраза (2-4 ноти), 0.5-1с. |
| `crate_rare.mp3` | Розкриття ящика — рідкісний приз (гардероб) | Те саме, але яскравіше/довше, з "блиском" (дзвіночки), 0.8-1.5с. |
| `achievement.mp3` | Розблоковано досягнення | Фанфара-мініатюра, гордий короткий мотив, 0.6-1с. |
| `level_up.mp3` | Підвищення "рівня ухилянта" | Висхідна мелодійна фраза, святкова, 0.6-1.2с. |
| `notice_resolved.mp3` | Повістку успішно вирішено (хабар/довідка/переховатись/прикриття) | Полегшення — м'який видих-звук або низхідний приємний тон, 0.3-0.6с. |
| `notice_failed.mp3` | Повістку НЕ вдалось вирішити (провал) | Тривожний короткий сигнал, не надто різкий (грає часто), 0.3-0.5с. |
| `hospital_success.mp3` | Вилазка в лікарню вдалась (отримав направлення/документ) | Нейтрально-канцелярський звук — штамп, шурхіт паперу, 0.3-0.6с. |
| `hospital_caught.mp3` | Вилазка в лікарню провалилась ("спалився") | Швидкі кроки-втеча або короткий тривожний сигнал, 0.3-0.6с. |
| `disease_confirmed.mp3` | Хворобу внесено в медичну картку (фінал циклу) | Більш вагомий/урочистий, ніж craft.mp3 — коротка мелодійна фраза, 0.5-0.9с. |
| `legalization.mp3` | Успішна легалізація (разова подія, гра ЗАВЖДИ пробує його програти) | Найбільш "епічний" звук у грі — фанфари/салют-мініатюра, 1.5-3с. Це кульмінаційний момент, не шкодуй на нього. |
| `error.mp3` | Зарезервовано на майбутнє (загальна помилка дії) | Короткий нейтральний "бемс", 0.15-0.3с. Поки код цей файл ніде не викликає явно — залишив про запас. |

## Технічні нотатки (не для промптів, для тебе)

- Гучність у коді зафіксована на 0.5 (`a.volume = 0.5`) для всіх звуків
  одразу — якщо якийсь файл вийде значно тихішим/гучнішим за решту,
  нормалізуй гучність САМОГО файлу перед завантаженням, а не проси мене
  міняти код під кожен окремо.
- Перемикач "вимкнути звук" у налаштуваннях ще не зроблений (є заглушка
  `soundsMuted = false` в коді, завжди false) — якщо потрібен реальний
  перемикач у UI, скажи окремо, це швидко.
- Шлях `/public/sounds/` — треба буде створити цю папку, її ще нема в
  репозиторії.
