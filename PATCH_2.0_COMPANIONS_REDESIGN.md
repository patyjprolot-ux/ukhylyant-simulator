# PATCH 2.0 — Редизайн компаньйонів (NPC → довіра → квест → приєднання)

**Статус: ДИЗАЙН-ДОКУМЕНТ, НЕ ЗАДЕПЛОЄНО.** Написано на пряму вказівку
розробника ("розроби сам") після того, як `PATCH_2.0_CLAUDE_DECISIONS.md`
(доповнення до Р4) залишив це "деталі чекають окремого заходу". Тут ці
деталі є — повна специфікація, готова до кодування, — але жодна стрічка
коду ще не змінена. Перед реалізацією потрібне підтвердження розробника.

## 0. Що не так із поточною моделлю

`catalog/pets.js` — шість компаньйонів, кожен купується прямо за ТК:

| id | Ефект зараз | Ціна зараз |
|---|---|---|
| neighbor | −10% шанс облави, ×0.85 приріст розшуку | 3 000 |
| goose | +15% сила кліку | 8 000 |
| cat | +30% швидкість відновлення енергії | 6 000 |
| dog | +40% здобич із вилазок | 15 000 |
| rat | −50% ризик спалитись на вилазці | 25 000 |
| pigeon | −25% час вилазки, +15% шанс пройти чекпоінт | 40 000 |

Це рівно модель "купив → +5%" (насправді тут і до +50%), яку ГПТ і
DeepSeek одноголосно розкритикували, а розробник прямо не заперечив:
компаньйон — це разова покупка за нагромаджені ТК, без жодного зв'язку
із сюжетом чи стосунками з NPC. У гри вже є повністю робочий двигун
саме для "стосунків" — `REPUTATION_NPCS` (`catalog/social.js`) — але
компаньйони його ніяк не використовують.

## 1. Рішення: прив'язати кожного компаньйона до вже існуючого NPC

Замість шести нових персонажів (дорого й розмиває увагу) — компаньйони
розподілені між чотирма NPC, які вже живуть у грі (`nina`, `tolik`,
`mykola`, `oksana`, `catalog/social.js:53-94`), кожен з яких уже має
власний квестовий ланцюг і `perk` на 100 репутації. Це навмисно:
гравець, який уже качав репутацію заради існуючого перку, автоматично
наближається і до компаньйона — механіки підсилюють одна одну, а не
конкурують за час гравця.

Прив'язка (обґрунтування нижче):

| NPC | Компаньйон(и) | Чому саме цей NPC |
|---|---|---|
| Баба Ніна | 🧓 Сусідка-пліткарка (neighbor) | Ніна — буквально та сама "знає все про всіх" мережа чуток. Компаньйон — не новий персонаж, а **її товарка**, яку вона зводить із гравцем після того, як почала довіряти |
| Перекуп Толік | 🐕 Двірняга-нюхач (dog), 🐀 Щур-розвідник (rat) | Толік — сірий делец: пес, що винюхує здобич, і щур-інформатор — обидва прямо з його "кола знайомств". Найбільше стосується торгового/здобичного профілю Толіка |
| Дільничний Микола | 🦢 Бойовий Гусак (goose) | Іронія: формально страж закону "продає" тобі власного вартового гусака — того, хто мав би тебе ловити, натомість допомагає ховатись |
| Волонтерка Оксана | 🐈 Кіт-антистрес (cat), 🕊️ Голуб-курʼєр (pigeon) | Оксана возить гуманітарку і заспокоює людей — кіт логічно її. Голуб-кур'єр — волонтерська логістика, той самий профіль |

## 2. Новий цикл: quest → confirm → recruit (не instant-buy)

Для кожного компаньйона додається **один додатковий квест**, що
відкривається лише коли `repMaxed(user, npc.id)` (тобто після того, як
гравець уже вибив 100 репутації тим самим способом, що й зараз —
`donate`/`metric` квести з `catalog/social.js`). Формат квесту — той
самий рушій, нове поле `companionQuest` на записі NPC:

```js
// catalog/social.js — доповнення до REPUTATION_NPCS[i]
{
  id: 'nina', ...,
  companionQuest: {
    petId: 'neighbor',
    text: 'Ніна познайомить тебе із сусідкою, якщо принесеш їй у подяку 3 упаковки ліків',
    type: 'donate', res: 'meds', target: 3,
  },
},
```

Логіка (нова, `routes/misc.js`, поруч із існуючим `/api/reputation/claim`, `routes/misc.js:187`):

1. `GET /api/npc/companion-quest/:npcId` — повертає стан
   (`locked` доки `!repMaxed`, `available` коли відкрито й не виконано,
   `done` коли виконано) — компаньйон купувати не можна, поки
   `done !== true`.
2. `POST /api/npc/companion-quest/claim` — та сама перевірка
   ресурсу/метрики, що й у `/api/reputation/claim` (`routes/misc.js:187-207`)
   для звичайних квестів NPC, але замість `rep += quest.rep` виставляє
   `user.companionUnlocked[npcId] = true`.
3. `POST /api/pet/buy` (`routes/misc.js:19`) — додається перевірка:
   `if (!user.companionUnlocked?.[pet.npcId]) return { success:false, message:'Спочатку заслужи довіру NPC' }`.
   Ціна лишається окремим кроком ПІСЛЯ квесту — квест не замінює
   оплату, а розблоковує саму МОЖЛИВІСТЬ купівлі. Гравець, що вже
   вклав час у стосунки з NPC, усе одно платить — тепер уже свідомо,
   як символічну "дяку", а не сліпо за спред-шитом бонусів.

Це й реалізує "NPC → довіра → квест → приєднання" буквально в тому
порядку, що просив розробник: довіра (rep 100) → квест (донат/умова)
→ приєднання (оплата й екіпірування).

## 3. Нові числа: бонуси 1-3%, ціни ×3-5

Пряма вказівка з `PATCH_2.0_CLAUDE_DECISIONS.md` (доповнення до Р4).
Компаньйон перестає бути економічним важелем і стає **колекційною
дрібницею з мінімальним, ледь відчутним нахилом** — увесь реальний
прогрес далі йде через дерево навичок і крафт, не через "купив кота".

| id | Було | Стало | Множник у коді | Нова ціна (було × 4) |
|---|---|---|---|---|
| neighbor | −10% шанс облави | −2% | `PET_NEIGHBOR_RAID_MULT: 0.98` (було 0.9) | 12 000 |
| neighbor | ×0.85 приріст розшуку | ×0.98 | `HEAT_NEIGHBOR_MULT: 0.98` (було 0.85) | (та сама покупка) |
| goose | +15% сила кліку | +2% | `PET_GOOSE_CLICK_MULT: 1.02` (було 1.15) | 32 000 |
| cat | +30% енергорегенерація | +3% | `PET_CAT_ENERGY_MULT: 1.03` (було 1.3) | 24 000 |
| dog | +40% здобич з вилазок | +3% | `PET_EXPEDITION.dog.lootMult: 1.03` (було 1.4) | 60 000 |
| rat | −50% ризик вилазки | −3% | `PET_EXPEDITION.rat.riskMult: 0.97` (було 0.5) | 100 000 |
| pigeon | −25% час вилазки | −2% | `PET_EXPEDITION.pigeon.timeMult: 0.98` (було 0.75) | 160 000 |
| pigeon | +15% шанс чекпоінта | +2% | `CHECKPOINT_PIGEON_BONUS: 0.02` (було 0.15) | (та сама покупка) |

`SNITCH_RAT_REVEAL_CHANCE` (щур одразу палить стукача, 35%) —
**навмисно не чіпаю**: це не економічний мультиплікатор, а бінарна
соціальна механіка (PvP-інформація), поза формулюванням "бонуси
1-3%" — там 1-3% шансу розкрити донощика зробили б компаньйона
беззмістовним у цій конкретній ролі. Лишаю як є, позначаю під
питанням для розробника окремо.

## 4. Долі вже куплених компаньйонів (живі гравці)

Гра "ще на тестуванні" (цитата розробника з іншого рішення), тому
рекомендація — **без грандфазерингу**: перебалансувати числа для ВСІХ,
включно з тими, хто вже купив компаньйона за старою ціною/бонусом.
Технічно нічого міняти в `user.ownedPets`/`user.petId` не треба —
ефект підтягується сам, бо множники йдуть з `ECONOMY`, а не
записуються в профіль гравця під час покупки. Єдине, що варто —
одноразове сповіщення при наступному вході ("Компаньйони
збалансовані — новий ефект менший, зате тепер прив'язаний до довіри
NPC"), щоб не виглядало як мовчазний нерф.

## 5. Обсяг роботи (якщо розробник підтвердить)

- `catalog/social.js`: +1 поле `companionQuest` на 4 записи NPC.
- `catalog/pets.js`: +1 поле `npcId` на кожен запис.
- `catalog/economy.js`: 8 числових констант (таблиця вище).
- `catalog/expeditions.js`: 3 значення в `PET_EXPEDITION`.
- `routes/misc.js`: новий ендпоінт `companion-quest` (GET+POST) +
  guard у `/api/pet/buy`.
- `lib/user-store.js`: нове поле `companionUnlocked: {}` на профілі
  (аналогічно `reputation: {}`).
- Клієнт (`server.js`, вкладка компаньйонів): показати замок/квест
  замість кнопки "Купити", доки NPC-умова не виконана.
- Одне сповіщення для існуючих власників (п.4).

Оцінка: невеликий за обсягом код (весь двигун довіри/квестів уже є),
основна робота — саме цей документ і числа, а не імплементація.
